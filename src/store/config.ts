import { GlobalJSON } from "@/types/json";

export const defaultGlobalObj: GlobalJSON = ({
    type: 'page',
    title: '默认标题',
    routeName: 'default',
    initApi: "",
    content: [
        // json.Button,
        // json.Input
    ]
})